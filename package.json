{
  "name": "hugo-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "hugo --gc",
    "clean": "rm -rf public resources",
    "server": "hugo server --buildFuture --disableFastRender"
  }
}
